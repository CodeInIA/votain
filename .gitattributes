# Normalize line endings to LF on commit, checkout as LF everywhere
* text=auto eol=lf

# Binaries — never touch line endings
*.png binary
*.webp binary
*.jpg binary
*.svg binary
*.wasm binary
*.pdf binary
